import scala.collection.mutable.Buffer

class Place {
  
  var objects = Buffer[Mass]()
  
  def countMass(items: Mass): Int = {
    var weight = 0
    for(item <- items.) {
      if(item.toString == "I am a weight!") {
        weight += 1
        
      } else {
        h
      }
    }
  }
  
  
}